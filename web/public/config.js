/* Deploy override: set apiBase to your public API origin (no trailing slash).
 * Example: window.__PHI_DPS_CONFIG__ = { apiBase: "https://api.yourcompany.com" };
 * Leave empty to use REACT_APP_PHI_DPS_API_BASE from the build or the dev default. */
window.__PHI_DPS_CONFIG__ = window.__PHI_DPS_CONFIG__ || { apiBase: "" };
